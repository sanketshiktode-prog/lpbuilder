{
  "name": "pp-leads-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute pp-leads --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute pp-leads --local --file=schema.sql"
  },
  "devDependencies": { "wrangler": "^4" }
}
